let triangle = tile {
    self
} where {
    let self = regular_polygon(3);
    let left, bottom, right = self.edges;
};

let tri_pair = patch {
    lay triangle , triangle such_that $1.right <-> $2.right
};

tableau( num ) {
    lay tri_pair
}
